# ─────────────────────────────────────────────────────────────────────────────
# Immich worker node — environment variables
# Copy this file to .env and fill in the values that match your primary server.
# ─────────────────────────────────────────────────────────────────────────────

# Immich version — MUST match the tag running on your primary Immich server
# to avoid job-format or schema mismatches.
IMMICH_VERSION=release

# ─── Library mount ────────────────────────────────────────────────────────────
# HOST path on THIS machine (Chappie) where the Immich upload/library is accessible.
# This is mounted into the container at /opt/immich/uploads (matching the primary server).
# The worker needs read+write access to the exact same data as the primary server.
#
# Typical options:
#   SMB share mounted inside WSL2, e.g.:   /mnt/immich_library
#   NFS mount inside WSL2, e.g.:           /mnt/nfs/immich_library
UPLOAD_LOCATION=/mnt/immich_library

# ─── Primary Immich database (Proxmox VM) ─────────────────────────────────────
DB_HOSTNAME=10.10.40.40
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=
DB_DATABASE_NAME=immich

# ─── Primary Immich Redis (Proxmox VM) ────────────────────────────────────────
REDIS_HOSTNAME=10.10.40.40
REDIS_PORT=6379

# ─── Timezone ─────────────────────────────────────────────────────────────────
TZ=Europe/Copenhagen
