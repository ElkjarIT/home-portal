# Infisical Self-Hosted — Environment Variables
# Copy to .env and fill in real values (or use 1Password CLI, see README).
#
# NEVER commit .env — it is gitignored.

# ── Core secrets ──────────────────────────────────────────────
# Generate both with:  openssl rand -hex 16
ENCRYPTION_KEY=<generate-with-openssl-rand-hex-16>
AUTH_SECRET=<generate-with-openssl-rand-hex-16>

# ── PostgreSQL ────────────────────────────────────────────────
DB_USERNAME=infisical
DB_PASSWORD=<generate-strong-password>
DB_NAME=infisical

# ── Site URL (used for CORS, OAuth callbacks, email links) ───
# Set to the URL behind your reverse proxy:
SITE_URL=https://infisical.aser.dk

# ── SMTP (optional — uncomment in docker-compose.yml too) ────
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USERNAME=
# SMTP_PASSWORD=
# SMTP_FROM_ADDRESS=infisical@aser.dk
